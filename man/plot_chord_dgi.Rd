% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_chord.R
\name{plot_chord_dgi}
\alias{plot_chord_dgi}
\title{Draw a circlize chord diagram of drug–gene interactions}
\usage{
plot_chord_dgi(
  edges,
  drug_class_colors = repurp_class_colors(),
  gene_color = "#009E73",
  title = "Drug–Gene Chord Diagram",
  out_dir = NULL,
  base_filename = paste0("DGI_chord_", Sys.Date()),
  svg_w = 32,
  svg_h = 32,
  transparency = 0.15,
  min_drug_targets = 1L
)
}
\arguments{
\item{edges}{A data frame with columns \code{drug}, \code{gene}, \code{class} (drug class
string), and \code{int_type} (interaction-type string).  Typically built by
combining \code{\link[=repurp_ad_edges]{repurp_ad_edges()}} with DGIdb edges; see the package vignette.}

\item{drug_class_colors}{Named character vector mapping drug-class strings to
hex colours.  Defaults to \code{\link[=repurp_class_colors]{repurp_class_colors()}}.}

\item{gene_color}{Hex colour for gene sectors. Default \code{"#009E73"}.}

\item{title}{Plot title. Default \code{"Drug–Gene Chord Diagram"}.}

\item{out_dir}{Directory for saved SVG file. \code{NULL} = draw to current device only.}

\item{base_filename}{Base filename without extension.
Default \code{paste0("DGI_chord_", Sys.Date())}.}

\item{svg_w, svg_h}{SVG dimensions in inches. Defaults \code{32} / \code{32}.}

\item{transparency}{Chord transparency (0–1). Default \code{0.15}.}

\item{min_drug_targets}{Integer; drugs with fewer gene targets than this are
dropped to reduce clutter. Default \code{1L} (keep all).}
}
\value{
Invisibly returns the adjacency matrix used for plotting.
}
\description{
Renders a circular chord diagram (via the \code{circlize} package) showing
drug–biomarker interaction edges. Drug sectors are coloured by drug class and
grouped into a continuous colour band; gene sectors are coloured green.
Link ribbon colour encodes interaction type.
}
\examples{
\dontrun{
edges <- dplyr::bind_rows(repurp_ad_edges(), my_dgidb_edges)
plot_chord_dgi(edges, out_dir = "results/")
}
}
