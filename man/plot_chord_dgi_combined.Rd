% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_chord_combined.R
\name{plot_chord_dgi_combined}
\alias{plot_chord_dgi_combined}
\title{Draw a combined chord diagram of curated AD drugs + DGIdb hub drugs}
\usage{
plot_chord_dgi_combined(
  dgi_edges,
  curated_edges,
  drug_class_colors = repurp_class_colors(),
  gene_color = "#009E73",
  title = "Drug–Gene Chord Diagram — AD + DGIdb",
  out_dir = NULL,
  base_filename = paste0("DGI_chord_combined_", Sys.Date()),
  svg_w = 32,
  svg_h = 32,
  transparency = 0.15,
  min_dgidb_targets = 2L
)
}
\arguments{
\item{dgi_edges}{A data frame with columns \code{drug}, \code{gene}, \code{int_type}
(interaction type). Typically built from DGIdb non-AD drug data.}

\item{curated_edges}{A data frame with columns \code{drug}, \code{gene}, \code{class}
(drug class), and \code{int_type} (set to \code{"Curated MOA"}). Typically from
\code{\link[=repurp_ad_edges]{repurp_ad_edges()}} joined with \code{\link[=repurp_ad_drugs]{repurp_ad_drugs()}}.}

\item{drug_class_colors}{Named character vector mapping drug-class strings to
hex colours.  Defaults to \code{\link[=repurp_class_colors]{repurp_class_colors()}}.}

\item{gene_color}{Hex colour for gene sectors. Default \code{"#009E73"}.}

\item{title}{Plot title. Default \code{"Drug–Gene Chord Diagram"}.}

\item{out_dir}{Directory for saved SVG file. \code{NULL} = draw to current device only.}

\item{base_filename}{Base filename without extension.
Default \code{paste0("DGI_chord_", Sys.Date())}.}

\item{svg_w, svg_h}{SVG dimensions in inches. Defaults \code{32} / \code{32}.}

\item{transparency}{Chord transparency (0–1). Default \code{0.15}.}

\item{min_dgidb_targets}{Minimum number of gene targets for a DGIdb drug to be
included. Default \code{2}.}
}
\value{
Invisibly returns the adjacency matrix used for plotting.
}
\description{
Builds a \href{https://cran.r-project.org/package=circlize}{circlize} chord diagram
that merges (1) curated AD drug–biomarker edges from \code{\link[=repurp_ad_edges]{repurp_ad_edges()}} and
(2) DGIdb non-AD drugs filtered to those with at least \code{min_dgidb_targets}
gene connections. Drug sectors are coloured by drug class / interaction type;
gene sectors are green. Link colour encodes interaction type.
}
\examples{
\dontrun{
curated <- repurp_ad_edges() |>
  dplyr::filter(to \%in\% my_genes) |>
  dplyr::left_join(repurp_ad_drugs(), by = c("from" = "drug_name")) |>
  dplyr::rename(drug = from, gene = to, class = category) |>
  dplyr::mutate(int_type = "Curated MOA")

dgi <- dplyr::transmute(my_dgidb_data,
  drug = Drug, gene = Gene, int_type = InteractionType
)

plot_chord_dgi_combined(curated_edges = curated, dgi_edges = dgi, out_dir = "results/")
}
}
