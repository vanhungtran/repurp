% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_ad_network.R
\name{plot_dgi_ad_network}
\alias{plot_dgi_ad_network}
\title{Plot an AD treatment drug–biomarker network with drug categories}
\usage{
plot_dgi_ad_network(
  edges,
  drug_info = repurp_ad_drugs(),
  title = "AD Treatment Drug–Biomarker Network",
  gene_color = "#009E73",
  out_dir = NULL,
  base_filename = paste0("AD_drug_network_", Sys.Date()),
  width = 18,
  height = 16,
  dpi = 300
)
}
\arguments{
\item{edges}{A data frame with columns \code{from} (drug), \code{to} (gene),
\code{edge_type} (\code{"direct"}, \code{"indirect"}, or a DGIdb interaction type string),
and \code{mechanism} (description string). Typically built by combining
\code{\link[=repurp_ad_edges]{repurp_ad_edges()}} with optional DGIdb matches.}

\item{drug_info}{A data frame with columns \code{drug_name}, \code{brand}, \code{category}.
Defaults to \code{\link[=repurp_ad_drugs]{repurp_ad_drugs()}}.}

\item{title}{Plot title.}

\item{gene_color}{Hex colour for biomarker gene nodes. Default \code{"#009E73"}.}

\item{out_dir}{Directory for saved files. \code{NULL} = draw to current device only.}

\item{base_filename}{Base filename without extension.}

\item{width, height}{Plot dimensions in inches. Defaults \code{18} / \code{16}.}

\item{dpi}{Resolution for PNG. Default \code{300}.}
}
\value{
Invisibly returns the \code{ggplot} object.
}
\description{
Builds a publication-quality network showing established atopic-dermatitis (AD)
treatments (source: \code{\link[=repurp_ad_drugs]{repurp_ad_drugs()}}) connected to biomarker genes via
curated mechanism-of-action edges (source: \code{\link[=repurp_ad_edges]{repurp_ad_edges()}}).
}
\details{
The plot uses \code{igraph} + \code{qgraph} Fruchterman-Reingold layout, rendered with
\code{ggplot2}, \code{ggrepel} labels, and \code{ggnewscale} for separate legends.
Drug nodes are coloured by therapeutic category; biomarker nodes are green.
Solid edges = direct molecular target / DGIdb confirmed;
dashed edges = pathway-level (indirect) regulation.
}
\examples{
\dontrun{
edges <- repurp_ad_edges() |>
  dplyr::filter(to \%in\% my_genes)

plot_dgi_ad_network(
  edges    = edges,
  drug_info = repurp_ad_drugs(),
  out_dir  = "results/"
)
}
}
