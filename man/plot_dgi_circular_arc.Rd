% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_circular_arc.R
\name{plot_dgi_circular_arc}
\alias{plot_dgi_circular_arc}
\title{Plot a circular arc / edge-bundle drug–gene network}
\usage{
plot_dgi_circular_arc(
  edges,
  title = "Drug–Biomarker Interaction Network",
  drug_class_colors = repurp_class_colors(),
  gene_color = "#009E73",
  drug_color = "#E41A1C",
  seed = 42L,
  out_dir = NULL,
  base_filename = paste0("DGI_circular_arc_", Sys.Date()),
  width = 16,
  height = 16,
  dpi = 300
)
}
\arguments{
\item{edges}{A data frame with columns \code{from} (drug), \code{to} (gene), and
\code{class} (drug class string, used for edge colouring). Typically built by
joining \code{\link[=repurp_ad_edges]{repurp_ad_edges()}} with \code{\link[=repurp_ad_drugs]{repurp_ad_drugs()}}.}

\item{title}{Plot title.}

\item{drug_class_colors}{Named character vector mapping drug class strings
to hex colours. Defaults to \code{\link[=repurp_class_colors]{repurp_class_colors()}}.}

\item{gene_color}{Hex colour for gene nodes. Default \code{"#009E73"}.}

\item{drug_color}{Hex colour for drug nodes. Default \code{"#E41A1C"}.}

\item{seed}{Random seed for reproducible layout. Default \code{42}.}

\item{out_dir}{Directory for saved files. \code{NULL} = draw to current device.}

\item{base_filename}{Base filename without extension.}

\item{width, height}{Plot dimensions in inches. Defaults \code{16} / \code{16}.}

\item{dpi}{Resolution for PNG. Default \code{300}.}
}
\value{
Invisibly returns the \code{ggplot} object.
}
\description{
Draws a circular network where drugs and genes alternate around the
circumference, connected by curved arc edges. Drug edges are coloured by
drug class. This layout is compact and well-suited for posters or
presentations where space is tight.
}
\examples{
\dontrun{
edges <- repurp_ad_edges() |>
  dplyr::filter(to \%in\% my_genes) |>
  dplyr::left_join(repurp_ad_drugs(), by = c("from" = "drug_name")) |>
  dplyr::rename(class = category)

plot_dgi_circular_arc(edges, out_dir = "results/")
}
}
