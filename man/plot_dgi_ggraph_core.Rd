% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_ggraph.R
\name{plot_dgi_ggraph_core}
\alias{plot_dgi_ggraph_core}
\title{Plot a core Drug–Gene–Pathway network (high-connectivity nodes only)}
\usage{
plot_dgi_ggraph_core(
  dgi_edges,
  pathway_edges = NULL,
  title = "Core Drug–Gene Network",
  subtitle = "Genes with ≥ 3 edges | All drugs and pathways labelled",
  caption = NULL,
  min_gene_edges = 3L,
  seed = 42L,
  layout = "kk",
  out_dir = NULL,
  base_filename = paste0("DGI_ggraph_core_", Sys.Date()),
  width = 16,
  height = 14,
  dpi = 300
)
}
\arguments{
\item{dgi_edges}{A data frame with columns \code{from} (gene), \code{to} (drug), \code{edge_type}
(interaction type string). Typically obtained by calling
\code{dplyr::select(net_dat, from = Gene, to = Drug, edge_type = InteractionType)}.}

\item{pathway_edges}{Optional data frame with columns \code{from} (gene), \code{to} (pathway
description), \code{edge_type} (pathway category, e.g. "KEGG"). Pass \code{NULL} (default)
to omit pathways.}

\item{title}{Plot title. Default \code{"Drug–Gene Network"}.}

\item{subtitle}{Optional subtitle string. Auto-generated when \code{NULL} (default).}

\item{caption}{Optional caption. Auto-generated when \code{NULL}.}

\item{min_gene_edges}{Minimum edge count for a gene to be included. Default \code{3}.}

\item{seed}{Random seed for reproducible layout. Default \code{42}.}

\item{layout}{ggraph layout algorithm. Default \code{"fr"} (Fruchterman-Reingold).}

\item{out_dir}{Directory for saved files. \code{NULL} = plot to current device only.}

\item{base_filename}{Base filename without extension. Default \code{paste0("DGI_ggraph_", Sys.Date())}.}

\item{width, height}{Plot dimensions in inches. Defaults \code{20} / \code{18}.}

\item{dpi}{Resolution for PNG. Default \code{300}.}
}
\value{
Invisibly returns the \code{ggplot} object.
}
\description{
Filters to genes with at least \code{min_gene_edges} edges before plotting,
which reduces clutter for large networks. All drug and pathway labels are shown.
}
\examples{
\dontrun{
dgi <- dplyr::select(net_dat, from = Gene, to = Drug, edge_type = InteractionType)
plot_dgi_ggraph_core(dgi, out_dir = "results/", min_gene_edges = 3)
}
}
