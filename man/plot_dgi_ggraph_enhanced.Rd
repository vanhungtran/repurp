% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_ggraph_enhanced.R
\name{plot_dgi_ggraph_enhanced}
\alias{plot_dgi_ggraph_enhanced}
\title{Plot an enhanced Drug–Gene network with ggraph + qgraph FR layout + ggrepel}
\usage{
plot_dgi_ggraph_enhanced(
  dgi_edges,
  title = "Drug–Gene Interaction Network",
  mode = c("full", "core"),
  min_gene_edges = 3L,
  min_drug_targets = 2L,
  seed = 42L,
  out_dir = NULL,
  base_filename = paste0("DGI_ggraph_enhanced_", Sys.Date()),
  width = 20,
  height = 18,
  dpi = 300
)
}
\arguments{
\item{dgi_edges}{A data frame with columns \code{from} (gene), \code{to} (drug),
\code{edge_type} (interaction type string). An optional \code{score} column
is used for edge width scaling.}

\item{title}{Plot title.}

\item{mode}{Either \code{"full"} (show hub drugs labelled, genes with ≥ \code{min_gene_edges})
or \code{"core"} (filter to core genes, label all nodes). Default \code{"full"}.}

\item{min_gene_edges}{Minimum number of drug connections for a gene to be
included in core mode, or labelled in full mode.}

\item{min_drug_targets}{Minimum number of gene targets for a drug to be labelled
in full mode. Default \code{2}.}

\item{seed}{Random seed for reproducible layout.}

\item{out_dir}{Directory for saved files. \code{NULL} = plot to current device only.}

\item{base_filename}{Base filename without extension.}

\item{width, height}{Plot dimensions in inches. Defaults \code{20} / \code{18}.}

\item{dpi}{Resolution for PNG. Default \code{300}.}
}
\value{
Invisibly returns the \code{ggplot} object.
}
\description{
Builds a publication-quality bipartite drug–gene network using a manual
Fruchterman-Reingold layout computed by \code{qgraph}, rendered with \code{ggraph},
\code{ggrepel} labels, and \code{ggnewscale} for separate node-type and edge-type
colour legends.
}
\examples{
\dontrun{
dgi <- dplyr::transmute(net_dat, from = Gene, to = Drug,
                         edge_type = InteractionType, score = interaction_score)
plot_dgi_ggraph_enhanced(dgi, mode = "full", out_dir = "results/")
plot_dgi_ggraph_enhanced(dgi, mode = "core", out_dir = "results/", min_gene_edges = 3)
}
}
