% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_heatmap.R
\name{plot_dgi_heatmap}
\alias{plot_dgi_heatmap}
\title{Plot a Drug × Gene interaction tile heatmap}
\usage{
plot_dgi_heatmap(
  dgi_edges,
  drug_info = NULL,
  title = "Drug–Gene Interaction Heatmap",
  max_drugs = 60L,
  max_genes = 50L,
  edge_palette = dgi_edge_colors,
  out_dir = NULL,
  base_filename = paste0("DGI_heatmap_", Sys.Date()),
  width = 16,
  height = 14,
  dpi = 300
)
}
\arguments{
\item{dgi_edges}{A data frame with columns \code{from} (gene), \code{to} (drug),
\code{edge_type} (interaction type string).}

\item{drug_info}{Optional data frame with columns \code{drug_name} and \code{category}.
When supplied a colour strip on the left side shows drug class.
Defaults to \code{\link[=repurp_ad_drugs]{repurp_ad_drugs()}} if all drugs in \code{dgi_edges} match AD drugs;
set to \code{NULL} to suppress the strip entirely.}

\item{title}{Plot title.}

\item{max_drugs}{Maximum number of drugs shown (top by degree). Default \code{60L}.}

\item{max_genes}{Maximum number of genes shown (top by degree). Default \code{50L}.}

\item{edge_palette}{Named character vector of interaction type → hex colour.
Defaults to \link{dgi_edge_colors}.}

\item{out_dir}{Directory for saved files. \code{NULL} = plot to current device only.}

\item{base_filename}{Base filename without extension.}

\item{width, height}{Plot dimensions in inches. Defaults \code{16} / \code{14}.}

\item{dpi}{Resolution for PNG. Default \code{300}.}
}
\value{
Invisibly returns the \code{ggplot} object.
}
\description{
Renders a publication-quality ggplot2 tile heatmap where rows are drugs,
columns are genes, and fill encodes the dominant interaction type.
An optional left-side strip encodes drug class when \code{drug_info} is supplied.
Rows are sorted by drug class then degree; columns by gene degree.
}
\examples{
\dontrun{
dgi <- dplyr::transmute(net_dat, from = Gene, to = Drug, edge_type = InteractionType)
plot_dgi_heatmap(dgi, drug_info = repurp_ad_drugs(), out_dir = "results/")
}
}
