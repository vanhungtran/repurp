% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_igraph.R
\name{plot_dgi_igraph}
\alias{plot_dgi_igraph}
\title{Plot a drug-gene interaction network with igraph + Fruchterman-Reingold layout}
\usage{
plot_dgi_igraph(
  net_dat,
  title = "Drug-Gene Interaction Network",
  out_dir = NULL,
  base_filename = paste0("DGI_network_", Sys.Date()),
  gene_color = "#009E73",
  drug_color = "#F0E442",
  vertex_size = 4,
  label_cex = 0.5,
  area_factor = 2.05,
  repulse_factor = 3,
  svg_w = 20,
  svg_h = 18,
  png_w = 2000,
  png_h = 1750,
  save_svg = !is.null(out_dir),
  save_png = !is.null(out_dir)
)
}
\arguments{
\item{net_dat}{A data frame with columns \code{Gene}, \code{Drug}, \code{InteractionType} (and
optionally \code{Count} / other columns). Typically the output of \code{\link[=prepare_dgi_data]{prepare_dgi_data()}}.}

\item{title}{Character string for the network title (rendered via \code{text()}).}

\item{out_dir}{Directory path for saved files. If \code{NULL} (default) only the plot is
drawn to the current device; no files are written.}

\item{base_filename}{Base name (without extension) for saved files. Defaults to
\code{paste0("DGI_network_", Sys.Date())}.}

\item{gene_color}{Hex colour for gene nodes. Default \code{"#009E73"}.}

\item{drug_color}{Hex colour for drug nodes. Default \code{"#F0E442"}.}

\item{vertex_size}{Numeric size passed to \code{igraph::plot.igraph}. Default \code{4}.}

\item{label_cex}{Character expansion for node labels. Default \code{0.5}.}

\item{area_factor}{Exponent for the FR layout \code{area} parameter (\code{vcount^area_factor}).
Default \code{2.05}.}

\item{repulse_factor}{Exponent for the FR \code{repulse.rad} parameter. Default \code{3}.}

\item{svg_w, svg_h}{Width/height in inches for SVG output. Defaults \code{20} / \code{18}.}

\item{png_w, png_h}{Width/height in pixels for PNG output. Defaults \code{2000} / \code{1750}.}

\item{save_svg}{Logical; write an SVG file? Default \code{TRUE} when \code{out_dir} is given.}

\item{save_png}{Logical; write a PNG file? Default \code{TRUE} when \code{out_dir} is given.}
}
\value{
Invisibly returns a named list with elements \code{graph} (the \code{igraph} object)
and \code{layout} (the numeric layout matrix).
}
\description{
Draws a bipartite drug–gene network using an \code{igraph} graph object laid out with
\code{qgraph::qgraph.layout.fruchtermanreingold()}. Optionally saves the result as
SVG (vector, poster-quality) and/or PNG (raster preview).
}
\examples{
\dontrun{
net_dat <- prepare_dgi_data("interactions.tsv", my_genes)
plot_dgi_igraph(net_dat, title = "My DGI Network", out_dir = "results/")
}
}
