% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_sankey.R
\name{plot_dgi_sankey}
\alias{plot_dgi_sankey}
\title{Plot a Drug Class → Drug → Gene alluvial (Sankey) flow diagram}
\usage{
plot_dgi_sankey(
  dgi_edges,
  drug_info = repurp_ad_drugs(),
  title = "Drug Class → Drug → Gene Flow",
  min_connections = 1L,
  max_genes = 30L,
  fill_by = c("drug_class", "edge_type"),
  edge_palette = dgi_edge_colors,
  out_dir = NULL,
  base_filename = paste0("DGI_sankey_", Sys.Date()),
  width = 18,
  height = 14,
  dpi = 300
)
}
\arguments{
\item{dgi_edges}{A data frame with columns \code{from} (gene), \code{to} (drug),
\code{edge_type} (interaction type string).}

\item{drug_info}{A data frame with columns \code{drug_name} and \code{category}.
Defaults to \code{\link[=repurp_ad_drugs]{repurp_ad_drugs()}}.}

\item{title}{Plot title.}

\item{min_connections}{Minimum number of edges for a drug–gene pair to be
included. Increase to reduce clutter in large networks. Default \code{1L}.}

\item{max_genes}{Maximum number of genes shown (top by degree). Default \code{30L}.}

\item{fill_by}{One of \code{"drug_class"} (colour alluvium by drug category) or
\code{"edge_type"} (colour by interaction type). Default \code{"drug_class"}.}

\item{edge_palette}{Named hex vector for interaction types (used when
\code{fill_by = "edge_type"}). Defaults to \link{dgi_edge_colors}.}

\item{out_dir}{Directory for saved files. \code{NULL} = plot to current device only.}

\item{base_filename}{Base filename without extension.}

\item{width, height}{Plot dimensions in inches. Defaults \code{18} / \code{14}.}

\item{dpi}{Resolution for PNG. Default \code{300}.}
}
\value{
Invisibly returns the \code{ggplot} object.
}
\description{
Renders a three-axis alluvial plot showing how drug classes funnel through
individual drugs into biomarker genes. The width of each flow band is
proportional to the number of drug–gene connections. Requires the
\code{ggalluvial} and \code{ggplot2} packages.
}
\examples{
\dontrun{
dgi <- dplyr::transmute(net_dat, from = Gene, to = Drug, edge_type = InteractionType)
plot_dgi_sankey(dgi, drug_info = repurp_ad_drugs(), out_dir = "results/")
}
}
