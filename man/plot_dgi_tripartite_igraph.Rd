% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgi_tripartite_igraph.R
\name{plot_dgi_tripartite_igraph}
\alias{plot_dgi_tripartite_igraph}
\title{Plot a tripartite Drug–Gene–Pathway network with igraph + FR layout}
\usage{
plot_dgi_tripartite_igraph(
  dgi_edges,
  pathway_edges = NULL,
  title = "Drug–Gene–Pathway Network",
  out_dir = NULL,
  base_filename = paste0("DGI_tripartite_", Sys.Date()),
  gene_color = "#009E73",
  drug_color = "#F0E442",
  pathway_color = "#6A3D9A",
  vertex_size = 3,
  label_cex = 0.45,
  area_factor = 2.05,
  repulse_factor = 3,
  svg_w = 24,
  svg_h = 22,
  png_w = 2400,
  png_h = 2200,
  save_svg = !is.null(out_dir),
  save_png = !is.null(out_dir)
)
}
\arguments{
\item{dgi_edges}{A data frame with columns \code{from} (gene), \code{to} (drug),
\code{edge_type} (interaction type string), and optionally \code{source} (e.g. "DGIdb").}

\item{pathway_edges}{A data frame with columns \code{from} (gene), \code{to} (pathway
description), \code{edge_type} (category string, e.g. "KEGG"), and optionally
\code{source} (e.g. "STRING"). Pass \code{NULL} (default) to omit pathways.}

\item{title}{Plot title.}

\item{out_dir}{Directory for saved files. \code{NULL} = draw to current device only.}

\item{base_filename}{Base filename without extension.
Default \code{paste0("DGI_tripartite_", Sys.Date())}.}

\item{gene_color, drug_color, pathway_color}{Hex colours for the three node types.
Defaults \code{"#009E73"}, \code{"#F0E442"}, \code{"#6A3D9A"}.}

\item{vertex_size}{Numeric vertex size. Default \code{3}.}

\item{label_cex}{Character expansion for labels. Default \code{0.45}.}

\item{area_factor, repulse_factor}{FR layout exponents. Defaults \code{2.05} / \code{3}.}

\item{svg_w, svg_h, png_w, png_h}{SVG/PNG dimensions.}

\item{save_svg, save_png}{Logical; write output files?}
}
\value{
Invisibly returns a named list with elements \code{graph} (the \code{igraph}
object) and \code{layout} (the numeric layout matrix).
}
\description{
Builds a three-node-type network (Gene, Drug, Pathway) using \code{igraph} and
\code{qgraph} Fruchterman-Reingold layout. Pathway edges (e.g. from STRING
enrichment via \code{rbioapi::rba_string_enrichment()}) are passed as
a separate data frame.
}
\examples{
\dontrun{
# requires rbioapi for STRING enrichment
enrich <- rbioapi::rba_string_enrichment(ids = my_genes, species = 9606)
pw_edges <- dplyr::bind_rows(
  enrich[["KEGG"]]         |> dplyr::mutate(category = "KEGG"),
  enrich[["WikiPathways"]] |> dplyr::mutate(category = "WikiPathways"),
  enrich[["Process"]]      |> dplyr::mutate(category = "GO Process")
) |> dplyr::filter(fdr < 0.05) |>
  tidyr::unnest(inputGenes) |>
  dplyr::transmute(from = inputGenes, to = description, edge_type = category)

plot_dgi_tripartite_igraph(
  dgi_edges    = dplyr::transmute(net_dat, from = Gene, to = Drug, edge_type = InteractionType),
  pathway_edges = pw_edges,
  out_dir = "results/"
)
}
}
