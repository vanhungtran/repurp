% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_dgi_data.R
\name{prepare_dgi_data}
\alias{prepare_dgi_data}
\title{Prepare a drug-gene interaction data frame for network plotting}
\usage{
prepare_dgi_data(
  interactions,
  gene_list,
  source_col = NULL,
  interaction_score_col = NULL
)
}
\arguments{
\item{interactions}{A data frame with at least columns \code{gene_name}, \code{drug_name},
and \code{interaction_type} (DGIdb TSV format), \strong{or} the path to such a TSV file.}

\item{gene_list}{Character vector of gene symbols to retain.}

\item{source_col}{Optional name of a source-database column to keep (string).
Defaults to \code{"interaction_source_db_name"} when present, otherwise omitted.}

\item{interaction_score_col}{Optional column name for a numeric interaction score.
Kept as-is when present.}
}
\value{
A tibble with columns \code{Gene}, \code{Drug}, \code{InteractionType}, and optionally
\code{Source} and \code{interaction_score}.
}
\description{
Reads a DGIdb-style interactions TSV (or accepts a pre-loaded data frame) and
filters to a user-supplied gene list, returning a standardised four-column
data frame ready for all \verb{plot_dgi_*} functions.
}
\examples{
\dontrun{
net_dat <- prepare_dgi_data("interactions.tsv", gene_list = my_genes)
}
}
