% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repurposing_analysis.R
\name{score_drug_repurposing}
\alias{score_drug_repurposing}
\title{Rank candidate drugs by biomarker coverage and evidence weight}
\usage{
score_drug_repurposing(
  edges,
  biomarkers = NULL,
  gene_weights = NULL,
  drug_info = repurp_ad_drugs(),
  direction = c("auto", "gene_to_drug", "drug_to_gene"),
  interaction_weights = NULL,
  source_weights = NULL,
  direct_bonus = 1.5,
  min_targets = 1L
)
}
\arguments{
\item{edges}{Drug-gene interaction data frame.}

\item{biomarkers}{Optional character vector defining the biomarker universe
to score against. When omitted, all genes in \code{edges} are used.}

\item{gene_weights}{Optional named numeric vector, or a data frame with gene
and weight columns, giving biomarker importance weights. Missing genes use
weight \code{1}.}

\item{drug_info}{Optional data frame with \code{drug_name}, \code{category}, \code{brand},
and/or \code{targets_known} columns to attach to the output.}

\item{direction}{Direction for \code{from}/\code{to} edge lists; see
\code{\link[=standardize_dgi_edges]{standardize_dgi_edges()}}.}

\item{interaction_weights}{Optional named numeric vector weighting
interaction types.}

\item{source_weights}{Optional named numeric vector weighting evidence
sources.}

\item{direct_bonus}{Multiplicative bonus for edges whose interaction type is
\code{"direct"}. Defaults to \code{1.5}.}

\item{min_targets}{Minimum number of unique targeted biomarkers required in
the returned table.}
}
\value{
A tibble ranked by descending \code{score}.
}
\description{
Scores drugs by the weighted biomarker targets they cover. Use this after
\code{\link[=prepare_dgi_data]{prepare_dgi_data()}} or with any edge list accepted by
\code{\link[=standardize_dgi_edges]{standardize_dgi_edges()}}.
}
\examples{
data(example_dgi)
score_drug_repurposing(example_dgi, biomarkers = c("IL13", "IL4R", "JAK1"))
}
