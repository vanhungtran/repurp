% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repurposing_analysis.R
\name{standardize_dgi_edges}
\alias{standardize_dgi_edges}
\title{Standardise drug-gene interaction edge columns}
\usage{
standardize_dgi_edges(
  edges,
  gene_col = NULL,
  drug_col = NULL,
  interaction_col = NULL,
  source_col = NULL,
  direction = c("auto", "gene_to_drug", "drug_to_gene"),
  drug_info = repurp_ad_drugs()
)
}
\arguments{
\item{edges}{A data frame containing drug-gene interactions.}

\item{gene_col, drug_col, interaction_col, source_col}{Optional explicit column
names. When omitted, common column names are detected automatically.}

\item{direction}{Direction of \code{from}/\code{to} edge lists. \code{"gene_to_drug"} treats
\code{from} as gene and \code{to} as drug; \code{"drug_to_gene"} reverses them; \code{"auto"}
uses \code{drug_info} overlap when possible and otherwise assumes
gene-to-drug.}

\item{drug_info}{Optional drug metadata with a \code{drug_name} column, used only
for automatic direction detection.}
}
\value{
A tibble with columns \code{gene}, \code{drug}, \code{interaction_type}, and
optional \code{source}.
}
\description{
Converts common DGIdb and Repurp edge-list layouts into a consistent tibble
with \code{gene}, \code{drug}, \code{interaction_type}, and optional \code{source} columns. This
is useful before ranking candidate drugs or combining interaction tables from
different sources.
}
\examples{
data(example_dgi)
standardize_dgi_edges(example_dgi)
}
